# Core backend, enough to run the LITE experience: streaming chat, personas,
# long-term memory, relationship/day/proactive/journal logic. No GPU, no torch.
# For voice (STT/TTS), calls, and singing, also install requirements-voice.txt.
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
pyyaml==6.0.2
pydantic==2.10.4
python-multipart==0.0.32
numpy==2.2.1
chromadb==1.5.9
apscheduler==3.11.3
